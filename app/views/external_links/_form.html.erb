<%= form_with(model: external_link) do |form| %>
  <% if external_link.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(external_link.errors.count, "error") %> prohibited this external_link from being saved:</h2>

      <ul>
        <% external_link.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.label :link_type, style: "display: block" %>
    <%= form.text_field :link_type %>
  </div>

  <div>
    <%= form.label :linkable_id, style: "display: block" %>
    <%= form.text_field :linkable_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
