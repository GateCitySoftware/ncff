<%= form_with(model: stage_schedule) do |form| %>
  <% if stage_schedule.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(stage_schedule.errors.count, "error") %> prohibited this stage_schedule from being saved:</h2>

      <ul>
        <% stage_schedule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :id, style: "display: block" %>
    <%= form.text_field :id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.label :stage_id, style: "display: block" %>
    <%= form.text_field :stage_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
