<h1>Log In</h1>

<%= form_with(url: login_path, local: true) do |form| %>
  <div>
    <%= form.label :email %>
    <%= form.email_field :email, required: true %>
  </div>

  <%= form.submit "Send login link" %>
<% end %>